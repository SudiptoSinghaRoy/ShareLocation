<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Share Location</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="./stylesheets/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <link rel="stylesheet" href='./stylesheets/style.css'>
    <link rel="stylesheet" href='./ol_map_lib/v6.5.0/ol.css'>
    <link href="./stylesheets/all.min.css" rel="stylesheet">
    
    <script src='./javascripts/jquery-3.5.1.min.js'></script>
    <script src='./javascripts/popper.min.js'></script>
    <script src='./javascripts/bootstrap.min.js'></script>
    <%= javascript_pack_tag 'application' %>
    <script src = './ol_map_lib/v6.5.0/ol.js'></script>
  </head>

  <body>
    <nav class="navbar navbar-dark navbar-expand bg-dark justify-content-between">
      <div class="container-fluid">
          <div>
              <% if request.fullpath.gsub('/','') == '' %>
                <span class="d-none d-sm-inline px-1 heading">My Dashboard</span> 
              <% else %>  
                <span class="d-none d-sm-inline px-1 heading"><%= request.fullpath.gsub('/','').upcase %> Sharing</span>
                <a href="/" class="btn btn-link font-weight-bold">My Dashboard</a>
              <% end %>   
          </div>
           <ul class="nav navbar-nav">
              <li class="nav-item dropdown text-center">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="fa fa-user"></span><span class="d-none d-sm-inline px-1"><%= current_user.username.upcase %></span> </a>
                  <div class="dropdown-menu dropdown-menu-right">
                      <%= link_to "Log out", destroy_user_session_path, :method => :delete, :class => "dropdown-item" %>
                  </div>
              </li>
           </ul>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
